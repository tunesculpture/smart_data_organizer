name: smart_data_organizer
description: AI-powered Excel-like Data Cleaner for Android
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # File handling
  file_picker: 6.1.1
  path_provider: ^2.1.2
  path: ^1.9.0

  # Excel / CSV
  excel: ^4.0.2
  csv: ^6.0.0

  # Spreadsheet editor  (FIX #3: pinned stable version)
  pluto_grid: 8.0.0

  # State management
  provider: ^6.1.2

  # HTTP / OpenAI
  http: ^1.2.1

  # Local storage
  shared_preferences: ^2.2.3

  # Export
  pdf: ^3.10.8
  printing: ^5.12.0
  share_plus: ^9.0.0

  # UI  (FIX #5: pinned compatible spinkit)
  flutter_spinkit: 5.1.0
  google_fonts: ^6.2.1
  animate_do: ^3.3.4
  dotted_border: ^2.1.0

  # Permissions + storage
  permission_handler: ^11.3.1

  # AdMob
  google_mobile_ads: ^4.0.0

  # Utils
  intl: ^0.19.0
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  # FIX #8: removed missing assets/icons/ reference
